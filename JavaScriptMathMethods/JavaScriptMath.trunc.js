/* JavaScript Math.trunc()

Definition and Usage

The Math.trunc() method returns the integer part of a number.

The Math.trunc() method removes the decimals (does NOT round the number).

Syntax
Math.trunc(x)

Return Value
Type	Description
Number	The integer part of the number.

Example */
console.log(Math.trunc(8.76));

/* JavaScript Rounding Functions

The Math.abs() Method Returns the absolute value of x
The Math.ceil() Method Returns x, rounded upwards to the nearest integer
The Math.floor() Method Returns x, rounded downwards to the nearest integer
The Math.round() Method Rounds x to the nearest integer
The Math.fround() Method Returns the nearest (32-bit single precision) float representation of a number
The Math.trunc() Method Returns the integer part of a number (x)*/